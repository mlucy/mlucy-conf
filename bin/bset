#!/bin/bash
b=/sys/class/backlight/acpi_video0/brightness
if [[ "$1" == "" ]]; then
    cat $b
elif [[ "$1" == "up" ]]; then
    echo $((`cat $b`+1)) > $b
elif [[ "$1" == "down" ]]; then
    echo $((`cat $b`-1)) > $b
else
    echo "$1" > $b
fi
